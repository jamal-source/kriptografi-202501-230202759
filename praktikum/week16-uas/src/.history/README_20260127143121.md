# KoinAllIn ERC-20 Token Project

This project creates a TinyCoin (TNC) ERC-20 token using Python, Brownie, Web3.py, and Ganache.

## Prerequisites

- Python 3.8+
- Node.js and npm
- Microsoft Visual C++ 14.0 or greater (for Windows, install from https://visualstudio.microsoft.com/visual-cpp-build-tools/)

## Installation

1. Install Python dependencies:
   ```bash
   pip install eth-brownie web3
   ```

   If you encounter build errors, try installing in a virtual environment:
   ```bash
   python -m venv venv
   venv\Scripts\activate  
   pip install eth-brownie web3
   ```

2. Install Ganache:
   ```bash
   npm install -g ganache
   ```

3. Install Brownie dependencies:
   ```bash
   brownie pm install
   ```

## Project Structure

```
tinycoin-python/
├─ contracts/
│   └─ TinyCoin.sol
├─ scripts/
│   ├─ deploy.py
│   └─ interact.py
├─ build/
├─ brownie-config.yaml
├─ README.md
└─ TODO.md
```

## Usage

1. Start Ganache (local blockchain):
   ```bash
   ganache
   ```

2. Deploy the TinyCoin contract:
   ```bash
   brownie run scripts/deploy.py --network development
   ```

3. Interact with the contract (transfer tokens and check balances):
   ```bash
   brownie run scripts/interact.py --network development
   ```

## Contract Details

- **Name**: TinyCoin
- **Symbol**: TNC
- **Initial Supply**: 1000 TNC (deployed to the first account)
- **Decimals**: 18 (standard ERC-20)

## Scripts

- `deploy.py`: Deploys the TinyCoin contract with initial supply.
- `interact.py`: Transfers 100 TNC to the second account and displays balances.
