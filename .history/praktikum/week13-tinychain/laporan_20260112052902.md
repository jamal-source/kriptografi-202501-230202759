# Laporan Praktikum Kriptografi
Minggu ke-: 13
Topik: TinyChain
Nama: Jamaludin
NIM: 230202759
Kelas: 5IKRB

---

## 1. Tujuan
Setelah mengikuti praktikum ini, mahasiswa diharapkan mampu:
1. Menjelaskan peran hash function dalam blockchain.
2. Melakukan simulasi sederhana Proof of Work (PoW).
3. Menganalisis keamanan cryptocurrency berbasis kriptografi.

---

## 2. Dasar Teori
(Ringkas teori relevan (cukup 2–3 paragraf).  
Contoh: definisi cipher klasik, konsep modular aritmetika, dll.  )

---

## 3. Alat dan Bahan
- Python 3.x
- Visual Studio Code / editor lain
- Git dan akun GitHub
- Library hashlib (sudah tersedia di Python standar)

---

## 4. Langkah Percobaan
1. Membuat folder `praktikum/week13-tinychain/src/` dan `praktikum/week13-tinychain/screenshots/`.
2. Membuat file `tinychain.py` di folder `src/` dengan implementasi kelas Block dan Blockchain.
3. Menjalankan program dengan perintah `python tinychain.py` untuk mensimulasikan mining blok.
4. Mengambil screenshot hasil eksekusi program dan menyimpannya di folder `screenshots/`.
5. Mengupdate file `laporan.md` dengan hasil percobaan, jawaban pertanyaan, dan commit log.

---

## 5. Source Code
```python
import hashlib
import time

class Block:
    def __init__(self, index, previous_hash, data, timestamp=None):
        self.index = index
        self.timestamp = timestamp or time.time()
        self.data = data
        self.previous_hash = previous_hash
        self.nonce = 0
        self.hash = self.calculate_hash()

    def calculate_hash(self):
        value = str(self.index) + str(self.timestamp) + str(self.data) + str(self.previous_hash) + str(self.nonce)
        return hashlib.sha256(value.encode()).hexdigest()

    def mine_block(self, difficulty):
        while self.hash[:difficulty] != "0" * difficulty:
            self.nonce += 1
            self.hash = self.calculate_hash()
        print(f"Block mined: {self.hash}")

class Blockchain:
    def __init__(self):
        self.chain = [self.create_genesis_block()]
        self.difficulty = 4

    def create_genesis_block(self):
        return Block(0, "0", "Genesis Block")

    def get_latest_block(self):
        return self.chain[-1]

    def add_block(self, new_block):
        new_block.previous_hash = self.get_latest_block().hash
        new_block.mine_block(self.difficulty)
        self.chain.append(new_block)

# Uji coba blockchain
if __name__ == "__main__":
    my_chain = Blockchain()
    print("Mining block 1...")
    my_chain.add_block(Block(1, "", "Transaksi A → B: 10 Coin"))

    print("Mining block 2...")
    my_chain.add_block(Block(2, "", "Transaksi B → C: 5 Coin"))

    print("Blockchain:")
    for block in my_chain.chain:
        print(f"Block {block.index}: {block.hash}")
```

---

## 6. Hasil dan Pembahasan
(- Lampirkan screenshot hasil eksekusi program (taruh di folder `screenshots/`).  
- Berikan tabel atau ringkasan hasil uji jika diperlukan.  
- Jelaskan apakah hasil sesuai ekspektasi.  
- Bahas error (jika ada) dan solusinya. 

Hasil eksekusi program Caesar Cipher:

![Hasil Eksekusi](screenshots/output.png)
![Hasil Input](screenshots/input.png)
![Hasil Output](screenshots/output.png)
)

---

## 7. Jawaban Pertanyaan
- Pertanyaan 1: Apa peran hash function dalam blockchain?  
  Hash function digunakan untuk menghasilkan hash unik untuk setiap blok, yang menghubungkan blok-blok dalam rantai. Hash juga memastikan integritas data karena perubahan kecil pada data akan menghasilkan hash yang sangat berbeda.

- Pertanyaan 2: Bagaimana Proof of Work (PoW) bekerja dalam cryptocurrency?  
  PoW adalah mekanisme konsensus di mana miner harus menemukan nonce yang membuat hash blok dimulai dengan sejumlah nol tertentu. Proses ini membutuhkan komputasi intensif untuk mencegah serangan dan memastikan keamanan jaringan.
---

## 8. Kesimpulan
Praktikum TinyChain berhasil mengimplementasikan simulasi sederhana blockchain dengan Proof of Work. Mahasiswa dapat memahami bagaimana hash function dan PoW bekerja dalam cryptocurrency. Percobaan ini menunjukkan bahwa blockchain dapat digunakan untuk mencatat transaksi secara aman dan terdesentralisasi.

---

## 9. Daftar Pustaka
- Nakamoto, S. (2008). Bitcoin: A Peer-to-Peer Electronic Cash System.
- Katz, J., & Lindell, Y. *Introduction to Modern Cryptography*.
- Stallings, W. *Cryptography and Network Security*.

---

## 10. Commit Log
```
commit b9f2405bb10c918d63e5b6ef5589ba7786bbe3e2
Author:     Jamaludin <jud2272@gmail.com>
Date: Mon Jan 12 05:25:41 2026 +0700

    week13-tinychain
```
