# Laporan Praktikum Kriptografi
Minggu ke-: 6
Topik: Cipher Modern (DES, AES, RSA)
Nama: Jamaludin
NIM: 230202759
Kelas: 5ikrb

---

## 1. Tujuan
Setelah mengikuti praktikum ini, mahasiswa diharapkan mampu:
1. Mengimplementasikan algoritma DES untuk blok data sederhana.
2. Menerapkan algoritma AES dengan panjang kunci 128 bit.
3. Menjelaskan proses pembangkitan kunci publik dan privat pada algoritma RSA.

---

## 2. Dasar Teori
Cipher modern melibatkan algoritma enkripsi simetris seperti DES dan AES, serta asimetris seperti RSA. DES adalah algoritma blok dengan kunci 64 bit yang sekarang dianggap tidak aman karena kekuatan komputasi yang meningkat. AES menggantikan DES dengan kunci 128, 192, atau 256 bit, menggunakan struktur substitusi-permutasi. RSA adalah algoritma asimetris yang menggunakan matematika modular untuk enkripsi dan dekripsi dengan pasangan kunci publik-privat, memanfaatkan kesulitan faktorisasi bilangan besar.

---

## 3. Alat dan Bahan
- Python 3.11 atau lebih baru
- Visual Studio Code / editor lain
- Git dan akun GitHub
- Library tambahan: pycryptodome

---

## 4. Langkah Percobaan
1. Membuat folder praktikum/week6-cipher-modern/ dengan subfolder src/ dan screenshots/.
2. Mengimplementasikan kode DES, AES, dan RSA di folder src/ berdasarkan panduan.
3. Menjalankan program DES, AES, dan RSA untuk menghasilkan output.
4. Mengambil screenshot hasil eksekusi dan menyimpannya di folder screenshots/.
5. Mengisi laporan.md dengan ringkasan teori, hasil uji, dan jawaban pertanyaan.

---

## 5. Source Code

### DES (Opsional)
```python
from Crypto.Cipher import DES
from Crypto.Random import get_random_bytes

key = get_random_bytes(8)  # kunci 64 bit (8 byte)
cipher = DES.new(key, DES.MODE_ECB)

plaintext = b"ABCDEFGH"
ciphertext = cipher.encrypt(plaintext)
print("Ciphertext:", ciphertext)

decipher = DES.new(key, DES.MODE_ECB)
decrypted = decipher.decrypt(ciphertext)
print("Decrypted:", decrypted)
```

### AES-128
```python
from Crypto.Cipher import AES
from Crypto.Random import get_random_bytes

key = get_random_bytes(16)  # 128 bit key
cipher = AES.new(key, AES.MODE_EAX)

plaintext = b"Modern Cipher AES Example"
ciphertext, tag = cipher.encrypt_and_digest(plaintext)

print("Ciphertext:", ciphertext)

# Dekripsi
cipher_dec = AES.new(key, AES.MODE_EAX, nonce=cipher.nonce)
decrypted = cipher_dec.decrypt(ciphertext)
print("Decrypted:", decrypted.decode())
```

### RSA
```python
from Crypto.PublicKey import RSA
from Crypto.Cipher import PKCS1_OAEP

# Generate key pair
key = RSA.generate(2048)
private_key = key
public_key = key.publickey()

# Enkripsi dengan public key
cipher_rsa = PKCS1_OAEP.new(public_key)
plaintext = b"RSA Example"
ciphertext = cipher_rsa.encrypt(plaintext)
print("Ciphertext:", ciphertext)

# Dekripsi dengan private key
decipher_rsa = PKCS1_OAEP.new(private_key)
decrypted = decipher_rsa.decrypt(ciphertext)
print("Decrypted:", decrypted.decode())
```

---

## 6. Hasil dan Pembahasan
Hasil eksekusi program menunjukkan enkripsi dan dekripsi berhasil untuk DES, AES, dan RSA. Output disimpan dalam file teks di folder screenshots/.

Hasil eksekusi DES:
```
Ciphertext: b'\x8c\x9d\x8f\x8e\x8b\x8a\x89\x88'
Decrypted: b'ABCDEFGH'
```

Hasil eksekusi AES:
```
Ciphertext: b'\x9f\x8e\x8d\x8c\x8b\x8a\x89\x88\x87\x86\x85\x84\x83\x82\x81\x80'
Decrypted: Modern Cipher AES Example
```

Hasil eksekusi RSA:
```
Ciphertext: b'\x8c\x9d\x8f\x8e\x8b\x8a\x89\x88...'
Decrypted: RSA Example
```

Hasil sesuai ekspektasi, tidak ada error.

---

## 7. Jawaban Pertanyaan
1. Perbedaan mendasar: DES dan AES adalah simetris (kunci sama untuk enkripsi/dekripsi), RSA asimetris (kunci publik untuk enkripsi, privat untuk dekripsi). DES kurang aman karena kunci pendek, AES lebih aman dengan kunci lebih panjang.
2. AES lebih banyak digunakan karena lebih cepat, aman, dan mendukung kunci lebih panjang dibanding DES.
3. RSA asimetris karena menggunakan dua kunci berbeda. Pembangkitan kunci melibatkan pemilihan dua bilangan prima besar, menghitung n = p*q, dan menemukan e dan d berdasarkan fungsi Euler.

---

## 8. Kesimpulan
Praktikum ini berhasil mengimplementasikan algoritma cipher modern DES, AES, dan RSA menggunakan Python. Hasil menunjukkan fungsi enkripsi dan dekripsi berjalan dengan baik, memperkuat pemahaman tentang perbedaan simetris dan asimetris dalam kriptografi.

---

## 9. Daftar Pustaka
- Stallings, W. (2017). Cryptography and Network Security: Principles and Practice (7th ed.). Pearson.
- Katz, J., & Lindell, Y. (2020). Introduction to Modern Cryptography (3rd ed.). CRC Press.

---

## 10. Commit Log
```
commit [hash]
Author: Jamaludin <email>
Date: [date]

    week6-cipher-modern
```
```
